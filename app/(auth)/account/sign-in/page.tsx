import { LoginCard } from '@/app/(auth)/account/sign-in/_components/login-card'

export default function LoginPage() {
  return (
    <section className='w-full min-h-screen flex items-start justify-center'>
      <LoginCard />
    </section>
  )
}
